[build]
builder = "NIXPACKS"
buildCommand = "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @yuristim/bot... build"

[deploy]
startCommand = "pnpm --filter @yuristim/bot start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
